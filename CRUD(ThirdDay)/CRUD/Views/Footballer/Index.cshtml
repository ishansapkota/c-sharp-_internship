@model List<CRUD.Models.FootballerDetails>
@*
    <h1>Footballers</h1>

    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Footballer Name</th>
                <th>Clubs</th>
                <th>Date of Birth</th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var footballer in Model)
            {
                <tr>
                    <td>@footballer.id</td>
                    <td>@footballer.Name</td>
                    <td>@footballer.Club</td>
                    <td>@footballer.DoB.ToString("dd-MM-yyyy")</td>
                    <td><a href="Footballer/View/@footballer.id" >View</a></td>
                </tr>
            }
        </tbody>
    </table>

*@
<section class="intro">
    <div class="bg-image h-100" style="background-color: #6095F0;">
        <div class="mask d-flex align-items-center h-100">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-12">
                        <div class="card shadow-2-strong" style="background-color: #f5f7fa;">
                            <div class="card-body">
                                <div class="table-responsive">
                                    <table class="table table-borderless mb-0">
                                        <thead>
                                            <tr>

                                                <th scope="col">ID</th>
                                                <th scope="col">FOOTBALLER NAME</th>
                                                <th scope="col">CLUB</th>
                                                <th scope="col">DATE OF BIRTH</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @foreach (var footballer in Model)
                                            {
                                                <tr>
                                                    <td>@footballer.id</td>
                                                    <td>@footballer.Name</td>
                                                    <td>@footballer.Club</td>
                                                    <td>@footballer.DoB.ToString("dd-MM-yyyy")</td>
                                                    <td><a href="Footballer/View/@footballer.id">View</a></td>
                                                </tr>
                                            }

                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<script src="~/js/view.js" asp-append-version="true"></script>
<link rel="stylesheet" href="~/css/view.css" />